import 'package:flutter/material.dart';

/// Extension for accessing localized strings in BuildContext
extension LocalizationExtension on BuildContext {
  // This will work after running: flutter gen-l10n
  // For now, fallback to AppStrings class is implemented in main.dart setup
}
